#!/bin/sh

echo "🔒 Running pre-commit validation..."
echo ""

# Run Mermaid diagram validation (staged files only)
echo "Step 1/3: Validating Mermaid diagrams..."
npm run validate:mermaid -- --staged || exit 1

# Run TypeScript type checking
echo "Step 2/3: Checking TypeScript types..."
npm run validate:types || exit 1

# Run build to ensure everything compiles
echo "Step 3/3: Running production build..."
npm run validate:build || exit 1

echo ""
echo "✅ All validations passed! Proceeding with commit."
